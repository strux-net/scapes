# Autogenerated makefile
# by :
#   m_tru mTR >makefile
#
m_diff=bash -c 'if ! diff -a -q $$1 ok/$$1 >/dev/null ; then echo -e "BAD\t$$1\tok/$$1" ; else echo -e "GOOD\t$$1" ; fi ; ' m_diff
m_copy=bash -c 'if ! diff -a -q $$1 $$2    >/dev/null ; then echo copying $$1 $$2; cp -p $$1 $$2 ; fi ; ' m_copy
ALL:\
  tagcloser_tru\

install install_ALL:\
  install_tagcloser_tru\

test test_ALL:\
  test_tagcloser_tru\

logged_build:
	-@${MAKE} --no-print-directory test_ALL 2>&1 | perl -pe 's,$$ENV{HOME}/,~/,g' >m.log # substitute /home/username with ~  (to avoid test-fails for trivial reasons)
	@${m_diff} m.log
confirmed_install: confirm install
confirm:
	@bash -c 'read -p "really install ? (^C) if not"'
export Title=a simple filter to close html-tags
install_tagcloser_tru:
	@echo  installing tagcloser_tru
	@${m_copy} tagcloser_tru ok/tagcloser_tru
	@${m_copy} statistics.out ok/statistics.out
	@${m_copy} test.out ok/test.out
	@${m_copy} docs/README.txt ok/docs/README.txt
	@${m_copy} docs/README.html ok/docs/README.html
	@${m_copy} tagcloser_tru ~/strux/bin/tagcloser_tru
test_tagcloser_tru: tagcloser_tru
	@echo  testing tagcloser_tru
	./tagcloser_tru test.in >test.out
	@${m_diff} test.out
	-./tagcloser_tru -invalid
.PHONY: tagcloser_tru
tagcloser_tru:
	@echo  building tagcloser_tru
	tru -s statistics.out ../proglang/common.tru ../proglang/statementDelimiter.tru ../proglang/plainvar.tru ../proglang/js/js.tru ../proglang/c_like_vars.tru tagcloser.tru ../lib/basicUSAGE.tru >tagcloser_tru
	@${m_diff} tagcloser_tru
	@${m_diff} statistics.out
	chmod +x tagcloser_tru
	{ echo "tagcloser_tru\n  a simple filter to close html-tags\n    usage"; ./tagcloser_tru -? 2>&1 | i -it 1 ; } >docs/README.txt
	@${m_diff} docs/README.txt
	{ sed "s/^    usage$$/    code/" docs/README.txt ; echo "style\n  Title\ta simple filter to close html-tags" ; } | html_tru >docs/README.html
	@${m_diff} docs/README.html

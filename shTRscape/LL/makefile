# Autogenerated makefile
# by :
#   m_tru mTR >makefile
#
m_diff=bash -c 'if ! diff -a -q $$1 ok/$$1 >/dev/null ; then echo -e "BAD\t$$1\tok/$$1" ; else echo -e "GOOD\t$$1" ; fi ; ' m_diff
m_copy=bash -c 'if ! diff -a -q $$1 $$2    >/dev/null ; then echo copying $$1 $$2; cp -p $$1 $$2 ; fi ; ' m_copy
ALL:\
  L\
  LLfs\
  LL\

install install_ALL:\
  install_L\
  install_LLfs\
  install_LL\

test test_ALL:\
  test_L\
  test_LLfs\
  test_LL\

logged_build:
	-@${MAKE} --no-print-directory ALL test_ALL 2>&1 | perl -pe 's,$$ENV{HOME}/,~/,g' >m.log # substitute /home/username with ~  (to avoid test-fails for trivial reasons)
	@${m_diff} m.log
confirmed_install: confirm install
confirm:
	@bash -c 'read -p "really install ? (^C) if not"'
export Title=A vim/perl based filebrowser with nice features
install_L:
	@echo  installing L
	@${m_copy} L ok/L
	@${m_copy} L ~/strux/bin/L
test_L: L
	@: # an empty action. To avoid nothing to be done for test_whatever
.PHONY: L
L:
	@echo  building L
	sh_tru L.shTR >L
	@${m_diff} L
	chmod +x L
install_LLfs:
	@echo  installing LLfs
	@${m_copy} LLfs ok/LLfs
	@${m_copy} LLfs ~/strux/bin/LLfs
test_LLfs: LLfs
	@: # an empty action. To avoid nothing to be done for test_whatever
.PHONY: LLfs
LLfs:
	@echo  building LLfs
	sh_tru LLfs.shTR >LLfs
	@${m_diff} LLfs
	chmod +x LLfs
install_LL:
	@echo  installing LL
	@${m_copy} LL ok/LL
	@${m_copy} LL ~/strux/bin/LL
test_LL: LL
	@: # an empty action. To avoid nothing to be done for test_whatever
.PHONY: LL
LL:
	@echo  building LL
	sh_tru LL.shTR >LL
	@${m_diff} LL
	chmod +x LL
